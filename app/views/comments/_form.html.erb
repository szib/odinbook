<%= form_with scope: :comment, url: post_comments_path(post), local: true do |form| %>
  <% if @comment && @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group ml-5 mr-3 mt-3 text-right">
    <%= form.text_area :content, class: 'form-control', placeholder: 'New comment...', size: '30x2' %>
    <%= render 'shared/send_btn' %>
  </div>
<% end %>
