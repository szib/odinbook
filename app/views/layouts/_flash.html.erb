<% flash.each do |key, value| %>
  <div class="alert<%= bootstrap_style_for(key) %>">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>
