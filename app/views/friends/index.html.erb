<div class="d-flex justify-content-center pt-3">
  <h1><%= current_user.profile.full_name %>'s friends</h1>
</div>

<div class="d-flex flex-wrap justify-content-center">

  <% @friends.each do |user| %>
    <%= render 'shared/user_card', user: user %>
  <% end %>

</div>
